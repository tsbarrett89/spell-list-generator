const initialState = {
    spells: [],
    savedSpells: [],
    isFetching: false,
    errorMessage: ''
}

const reducer = (state = initialState, action) => {
    return state
}

export default reducer